# AGENTS.md

请按以下固定要求处理本项目：

1. Python 环境使用 conda 环境：

   ```bash
   conda activate word_template
   ```

2. 运行 Python 脚本时，优先使用该环境下的 python，不要使用系统默认 python。

3. 调用 DeepSeek 时，都使用 `word_template_skill/.env` 里有效的 `DEEPSEEK_API_KEY`。

4. 修改代码时，尽量保持原有结构和风格，不要无关重构。

5. 修改完成后，请说明：
   - 改了哪些文件；
   - 为什么这样改；
   - 如何运行；
   - 是否需要我手动配置环境变量。
